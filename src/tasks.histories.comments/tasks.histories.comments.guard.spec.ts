import { TaskHistoryCommentGuard } from './tasks.histories.comments.guard';

describe('TasksHistoriesCommentsGuard', () => {
  it('should be defined', () => {
    expect(TaskHistoryCommentGuard).toBeDefined();
  });
});
